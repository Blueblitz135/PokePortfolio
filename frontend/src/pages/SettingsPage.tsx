import { PlaceholderPage } from "../components/PlaceholderPage";

export function SettingsPage() {
  return (
    <PlaceholderPage
      eyebrow="Portfolio preferences"
      title="Settings"
      description="Manage supported display preferences for this portfolio."
      message="Settings controls and persistence are intentionally not active yet."
    />
  );
}
