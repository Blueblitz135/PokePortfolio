import { PlaceholderPage } from "../components/PlaceholderPage";

export function SearchPage() {
  return (
    <PlaceholderPage
      eyebrow="Card discovery"
      title="Search"
      description="Find card metadata before adding an owned copy."
      message="Card search requests and add-to-collection actions are intentionally not active yet."
    />
  );
}
